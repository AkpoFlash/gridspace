'use strict';


/* Function for visible */

function width(element, percentOfWidth){
  element.style.width = winWidth * percentOfWidth + 'px';
}

function height(element, percentOfHeight){
  element.style.height = winHeight * percentOfHeight + 'px';
}
